---
title: "Corpse Party 2021 (PATCH ITA - UPDATE)"
layout: single
---
Sta procendendo bene, purtoppo' ci vuole tempo essendo che 
---------------
1. Bisogna ricontrollare tutti i dialoghi uno a uno
2. Ri-Adattare alcune line di dialogo per farli stare nel Text Box
3. Aggiustare i tools 


Infatti mano a mano sto aggiustado i tools, una parte che mi ha messo a dura prova
e separare alche parti, poiche si rompevano facilmente durante la fase di apllicazione della 
traduzione 

```python
def should_be_separate_line(line: str) -> bool:
    patterns = [
        r'^[^:]{1,80}:',  # Linee di dialogo
        r'^\*[^*]+\*',    # Effetti sonori
        r'^\[.*\]$',      # Testo tra parentesi quadre
        r'^(\d{1,2}\s*/\s*\d{1,2})',  # Date
        r'^".*"$',        # Testo tra virgolette
        r'^\.{3}',        # Linee che iniziano con ...
    ]
    return any(re.match(pattern, line) for pattern in patterns)
```

in piu', dentro i json della traduzione ci sono un botto di caratteri giapponesi 
(usati per debug oppure contenuto tagliato), quindi F per me, altro check da fare

```python
CJK_RE = re.compile(r'[\u3000-\u303F\u3040-\u309F\u30A0-\u30FF\u4E00-\u9FFF]')

passthrough = bool(CJK_RE.search(s) or re.search(r'\btest\b', s, flags=re.I))

is_passthrough_block = any(CJK_RE.search(line) or re.search(r'\btest\b', line, flags=re.I) for line in b['orig'])
```
![ttest](/assets/images/img1.png)
![ttest](/assets/images/img2.png)




